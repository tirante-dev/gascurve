# ct lint / helm lint values: ingress with an existing database Secret.
database:
  existingSecret: my-db
ingress:
  enabled: true
  host: gascurve.com
config:
  server:
    # Example pod CIDR seen by the API for the ingress-nginx controller.
    trusted_proxies:
      - 10.244.0.0/16
