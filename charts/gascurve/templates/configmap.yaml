apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gascurve.fullname" . }}-config
  labels:
    {{- include "gascurve.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
